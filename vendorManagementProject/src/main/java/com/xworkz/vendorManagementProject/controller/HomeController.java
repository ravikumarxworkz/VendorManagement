package com.xworkz.vendorManagementProject.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller
@RequestMapping("/")
public class HomeController {

	/*
	 * @GetMapping("/homePage") public String homePage() { return "index";
	 * 
	 * }
	 * 
	 * @GetMapping("/logInPage") public String logInPage() { return "logIn";
	 * 
	 * }
	 * 
	 * @GetMapping("/registerPage") public String registerPage() { return
	 * "register";
	 * 
	 * }
	 */
}
